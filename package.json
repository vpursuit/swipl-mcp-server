{
  "name": "@vpursuit/swipl-mcp-server",
  "version": "1.0.3",
  "type": "module",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "build": "tsc && chmod 755 build/index.js",
    "build:package": "node scripts/build-package.js",
    "start": "node build/index.js",
    "server": "npm run build && npm start",
    "inspect": "npx @modelcontextprotocol/inspector node build/index.js",
    "prepublishOnly": "npm run build:package",
    "lint": "eslint . --ext .ts,.js",
    "lint:fix": "eslint . --ext .ts,.js --fix",
    "format": "prettier --write \"**/*.{ts,js,json,yml,yaml}\"",
    "format:check": "prettier --check \"**/*.{ts,js,json,yml,yaml}\"",
    "publish:prepare": "npm run build:package",
    "publish:dry-run": "npm run publish:prepare && cd dist && npm publish --dry-run",
    "publish:npm": "npm run publish:prepare && cd dist && npm publish",
    "github:release": "node scripts/create-github-release.js",
    "version:patch": "npm version patch",
    "version:minor": "npm version minor",
    "version:major": "npm version major",
    "version:prerelease": "npm version prerelease",
    "release:patch": "npm run version:patch && git push origin main && git push --tags && npm run publish:npm && npm run github:release",
    "release:minor": "npm run version:minor && git push origin main && git push --tags && npm run publish:npm && npm run github:release",
    "release:major": "npm run version:major && git push origin main && git push --tags && npm run publish:npm && npm run github:release",
    "release:prerelease": "npm run version:prerelease && git push origin main && git push --tags && npm run publish:npm && npm run github:release"
  },
  "keywords": [
    "mcp",
    "prolog",
    "swi-prolog",
    "swipl",
    "model-context-protocol",
    "logic-programming"
  ],
  "author": "Peter Trebing",
  "license": "BSD-3-Clause",
  "description": "SWI-Prolog MCP Server with dual query modes and security layer",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vpursuit/swipl-mcp-server.git"
  },
  "homepage": "https://github.com/vpursuit/swipl-mcp-server#readme",
  "bugs": {
    "url": "https://github.com/vpursuit/swipl-mcp-server/issues"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.2",
    "@vpursuit/swipl-mcp-server": "^1.0.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.2.1",
    "@typescript-eslint/eslint-plugin": "^8.7.0",
    "@typescript-eslint/parser": "^8.7.0",
    "eslint": "^9.8.0",
    "eslint-config-prettier": "^9.1.0",
    "jest": "^30.0.5",
    "prettier": "^3.3.3",
    "ts-jest": "^29.4.1",
    "typescript": "^5.9.2"
  }
}
